name: CI

on:
  push:
    branches: [main, chore/audit-fixes]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - run: bun ci
      - run: bun run validate:content
      - run: bun run lint
      - run: bun run typecheck
      - run: bun run build
      - run: bun run test
