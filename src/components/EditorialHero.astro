---
interface Props { eyebrow: string; title: string; copy?: string; index?: string; }
const { title, copy } = Astro.props;
---
<section class="editorial-hero"><h1>{title}</h1>{copy && <p>{copy}</p>}</section>
